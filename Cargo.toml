[package]
name = "factorio-up"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4" }
ring = { version = "0.17" }
tar = { version = "0.4" }
ureq = { version = "3", default-features = false, features = ["rustls"] }
xz2 = { version = "0.1", features = ["static"] }
