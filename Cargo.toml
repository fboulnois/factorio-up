[package]
name = "factorio-up"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4" }
curl = { version = "0.4", features = ["static-curl"] }
ring = { version = "0.17" }
tar = { version = "0.4" }
xz2 = { version = "0.1", features = ["static"] }
